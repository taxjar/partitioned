source "http://rubygems.org"

gemspec

# gem 'bulk_data_methods', git: 'https://github.com/taxjar/bulk_data_methods.git', tag: 'v3.0.0', require: false # Bulk insert for ActiveRecord
gem 'bulk_data_methods', path: '../bulk_data_methods', require: false # Bulk insert for ActiveRecord
